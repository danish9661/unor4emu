// CTSU mutual-mode touch proof (see ra4m1_ctsu_mutual_ok).
// NOTE: reconstructed — original used raw FSP R_CTSU registers via the
// capacitive-sensor path; this sketch shows the Arduino-level shape.
// The vendored r4ctsu.bin remains the proof binary.
void setup() { pinMode(LED_BUILTIN, OUTPUT); digitalWrite(LED_BUILTIN, HIGH); }
void loop() {}
